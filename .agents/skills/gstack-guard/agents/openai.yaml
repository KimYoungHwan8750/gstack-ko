interface:
  display_name: "gstack-guard"
  short_description: "전체 안전 모드: 파괴적 명령어 경고 + 디렉토리 범위 편집 제한. /careful(rm -rf, DROP TABLE, force-push 등에 대한 경고)과 /freeze(지정된 디렉토리 외부 편집 차단)를..."
  default_prompt: "Use gstack-guard for this task."
policy:
  allow_implicit_invocation: true
