interface:
  display_name: "gstack-ship"
  short_description: "Ship 워크플로우: 베이스 브랜치 감지 + 병합, 테스트 실행, diff 리뷰, VERSION 범프, CHANGELOG 업데이트, 커밋, 푸시, PR 생성. \"ship\", \"deploy\", \"push to..."
  default_prompt: "Use gstack-ship for this task."
policy:
  allow_implicit_invocation: true
